\* TLC Configuration for ConsensusSystem

\* Specification to check
SPECIFICATION Spec

\* Constants
CONSTANTS
    Nodes = {n1, n2, n3}
    Values = {v1, v2}
    MaxFailures = 1
    NULL = NULL

\* Invariants to check (safety properties)
INVARIANTS
    TypeOK
    Agreement
    Validity
    Integrity

\* Properties to check (temporal/liveness properties)
\* PROPERTIES
\*     CanDecide

\* Uncomment to check full liveness (may not terminate if liveness cannot be guaranteed)
\* PROPERTIES
\*     EventualDecision
\*     EventualLeader

\* State constraint to limit state space exploration
\* CONSTRAINT StateConstraint

\* Symmetry to reduce state space
\* SYMMETRY Perms(Nodes)  \* Commented out - can uncomment if needed

\* Action constraints (optional)
\* ACTION_CONSTRAINT ActionConstraint

\* View to reduce state space (optional)
\* VIEW View
